pipeline {
  agent any
  stages {
    stage("build"){
      steps {
        echo 'building the application'
        
      } 
    
    }
   stage("deployment"){
      steps {
        echo 'deploying application the application'
        
      } 
    
    }
  
  }

  
}
